<Window
    x:Class="Fluxo.Installer.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="clr-namespace:Fluxo.Installer.Models"
    Title="Fluxo Installer"
    Width="720"
    Height="420"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="28" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            FontSize="42"
            FontWeight="Bold"
            Foreground="#111827"
            Text="{Binding Title}" />

        <TextBlock
            Grid.Row="1"
            Margin="0,8,0,0"
            FontSize="18"
            Foreground="#4B5563"
            Text="{Binding Tagline}" />

        <TextBlock
            Grid.Row="3"
            FontSize="13"
            Foreground="#374151"
            Text="Install Location" />

        <TextBox
            Grid.Row="4"
            Height="34"
            Margin="0,6,0,0"
            Text="{Binding InstallFolder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <DockPanel
            Grid.Row="5"
            Margin="0,16,0,0"
            LastChildFill="False">
            <TextBlock
                VerticalAlignment="Center"
                DockPanel.Dock="Left"
                Foreground="#4B5563"
                Text="{Binding StatusMessage}" />

            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button
                    Margin="0,0,10,0"
                    Padding="20,8"
                    Command="{Binding LaunchAppCommand}"
                    Content="Launch Fluxo (Rocket)">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding State}" Value="{x:Static models:InstallerState.FinishedSuccess}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button
                    Margin="0,0,10,0"
                    Padding="16,8"
                    Command="{Binding ChangeDirectoryCommand}"
                    Content="Change Directory" />
                <Button
                    Padding="24,8"
                    Command="{Binding InstallCommand}"
                    Content="Install" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>