# Quick Task 260414-b39 Summary

**Task:** Disable Next on budget allocation error; hide Fixed/Goals steps in sidebar when no spending sources
**Date:** 2026-04-14
**Commit:** aee5ec5

## What Was Done

**`StartupWizardVM.cs`:**
- Added `IsNextEnabled` property: `!(IsStep5Active && HasBudgetAllocationError)`
- `OnPropertyChanged(nameof(IsNextEnabled))` fired in `OnCurrentStepIndexChanged` and `ValidateBudgetAllocation()`
- `OnPropertyChanged(nameof(HasSpendingSources))` fired in `RefreshSpendingSourcesAsync()` so XAML visibility binding updates live

**`StartupWizardPopup.xaml`:**
- `PrimaryActionButtonStyle` gains a disabled trigger: `Opacity=0.4` when `IsEnabled=False`
- Next button: `IsEnabled="{Binding IsNextEnabled}"`
- Step 3 (Fixed expenses) sidebar Border: `Visibility="{Binding HasSpendingSources, Converter=BoolToVisibilityConverter}"`
- Step 4 (Savings goals) sidebar Border: `Visibility="{Binding HasSpendingSources, Converter=BoolToVisibilityConverter}"`

## Status: Complete
