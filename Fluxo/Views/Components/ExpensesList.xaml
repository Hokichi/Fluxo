<UserControl
    x:Class="Fluxo.Views.Components.ExpensesList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:customControls="clr-namespace:Fluxo.Resources.CustomControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Fluxo.Views.Components"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>

        <DataTemplate x:Key="ExpenseListTemplate">
            <customControls:SwipeRevealContainer>
                <!--  Left action: delete (revealed on swipe right)  -->
                <customControls:SwipeRevealContainer.LeftContent>
                    <customControls:BalloonButton
                        Width="36"
                        Height="36"
                        Margin="8,0,0,0"
                        VerticalAlignment="Center"
                        ButtonIcon="{StaticResource CircleMinusSolid}"
                        Command="{Binding DataContext.DeleteExpenseLogCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        CommandParameter="{Binding}"
                        DefaultBackground="{StaticResource Brush.Danger}"
                        HoveredBackground="{StaticResource Brush.Danger}"
                        IconSize="14" />
                </customControls:SwipeRevealContainer.LeftContent>

                <!--  Right action: info / expense detail (revealed on swipe left)  -->
                <customControls:SwipeRevealContainer.RightContent>
                    <customControls:BalloonButton
                        Width="36"
                        Height="36"
                        Margin="0,0,8,0"
                        VerticalAlignment="Center"
                        ButtonIcon="{StaticResource Info}"
                        Click="OnExpenseDetailButtonClick"
                        DefaultBackground="{StaticResource Brush.Background.Surface}"
                        HoveredBackground="{StaticResource Brush.Background.Hover}"
                        IconSize="14" />
                </customControls:SwipeRevealContainer.RightContent>

                <Grid>
                    <Border
                        x:Name="ExpenseItemBGBorder"
                        Padding="12,10"
                        Background="{StaticResource Brush.Background.Hover}"
                        CornerRadius="12"
                        Visibility="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListViewItem}, Path=IsMouseOver, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <Border
                        x:Name="ExpenseItemBorder"
                        Padding="12,10"
                        Background="Transparent"
                        CornerRadius="12">
                        <Grid
                            MaxWidth="{Binding RelativeSource={RelativeSource AncestorType=ListView}, Path=ActualWidth}"
                            HorizontalAlignment="Stretch"
                            ClipToBounds="True">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Margin="0,0,8,0" HorizontalAlignment="Stretch">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        FontFamily="{StaticResource Bold}"
                                        FontSize="13"
                                        Foreground="{StaticResource Brush.Text.Primary}"
                                        Text="{Binding Expense.Name}"
                                        TextTrimming="CharacterEllipsis"
                                        TextWrapping="NoWrap" />
                                </StackPanel>
                                <StackPanel Margin="0,8,0,0" Orientation="Horizontal">
                                    <TextBlock
                                        FontFamily="{StaticResource Regular}"
                                        FontSize="10"
                                        Foreground="{StaticResource Brush.Text.Muted}"
                                        Text="{Binding DeductedOn, Converter={StaticResource DateTimeToRelativeDateConverter}}" />

                                    <Border
                                        x:Name="ExpenseTagBorder"
                                        Margin="8,0,0,0"
                                        Padding="5,2"
                                        VerticalAlignment="Center"
                                        Background="{Binding Expense.ExpenseTag.HexCode}">
                                        <Border.CornerRadius>
                                            <MultiBinding Converter="{StaticResource CornerRadiusConverter}">
                                                <Binding Path="ActualWidth" RelativeSource="{RelativeSource Self}" />
                                                <Binding Path="ActualHeight" RelativeSource="{RelativeSource Self}" />
                                            </MultiBinding>
                                        </Border.CornerRadius>
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            FontFamily="{StaticResource Medium}"
                                            FontSize="10"
                                            Foreground="{Binding Expense.ExpenseTag.HexCode, Converter={StaticResource BrushToLighterBrushConverter}}"
                                            Text="{Binding Expense.ExpenseTag.Name}" />
                                    </Border>
                                </StackPanel>
                            </StackPanel>

                            <StackPanel
                                Grid.Column="1"
                                MinWidth="0"
                                HorizontalAlignment="Right">
                                <TextBlock
                                    HorizontalAlignment="Right"
                                    FontFamily="{StaticResource Bold}"
                                    FontSize="13"
                                    Foreground="{StaticResource Brush.Text.Primary}"
                                    Text="{Binding Amount, Converter={StaticResource NumberWithCommasConverter}, StringFormat={}${0}}" />
                                <TextBlock
                                    MaxWidth="120"
                                    Margin="0,8,0,0"
                                    HorizontalAlignment="Right"
                                    FontFamily="{StaticResource Regular}"
                                    FontSize="10"
                                    Foreground="{StaticResource Brush.Text.Muted}"
                                    Text="{Binding SpendingSource.Name}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </customControls:SwipeRevealContainer>
        </DataTemplate>
        <Style x:Key="ExpenseListHeaderText" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource Bold}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Muted}" />
        </Style>

        <Style x:Key="ExpenseListHeaderGridStyle" TargetType="Grid">
            <Setter Property="Margin" Value="12,0,12,8" />
        </Style>

        <Style
            x:Key="ExpenseListFadingScrollViewerStyle"
            BasedOn="{StaticResource {x:Type customControls:FadingScrollViewer}}"
            TargetType="{x:Type customControls:FadingScrollViewer}">
            <Setter Property="Margin" Value="0" />
            <Setter Property="FadedEdgeThickness" Value="20" />
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type customControls:FadingScrollViewer}">
                        <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="24" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Border x:Name="PART_ScrollContentPresenterContainer">
                                <ScrollContentPresenter
                                    x:Name="PART_ScrollContentPresenter"
                                    Margin="{TemplateBinding Padding}"
                                    CanContentScroll="{TemplateBinding CanContentScroll}"
                                    CanHorizontallyScroll="False"
                                    CanVerticallyScroll="False"
                                    Content="{TemplateBinding Content}" />
                            </Border>

                            <ScrollBar
                                x:Name="PART_VerticalScrollBar"
                                Grid.Row="0"
                                Grid.Column="1"
                                AutomationProperties.AutomationId="VerticalScrollBar"
                                Cursor="Arrow"
                                Maximum="{TemplateBinding ScrollableHeight}"
                                Minimum="0"
                                ViewportSize="{TemplateBinding ViewportHeight}"
                                Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" />

                            <ScrollBar
                                x:Name="PART_HorizontalScrollBar"
                                Grid.Row="1"
                                Grid.Column="0"
                                AutomationProperties.AutomationId="HorizontalScrollBar"
                                Cursor="Arrow"
                                Maximum="{TemplateBinding ScrollableWidth}"
                                Minimum="0"
                                Orientation="Horizontal"
                                ViewportSize="{TemplateBinding ViewportWidth}"
                                Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                                Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ExpenseListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <ContentPresenter />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ExpenseListViewStyle" TargetType="ListView">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Stretch" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="ClipToBounds" Value="True" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource ExpenseListViewItemStyle}" />
            <Setter Property="ItemTemplate" Value="{StaticResource ExpenseListTemplate}" />
            <Setter Property="ScrollViewer.CanContentScroll" Value="False" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled" />
            <EventSetter Event="PreviewMouseWheel" Handler="OnExpenseListPreviewMouseWheel" />
        </Style>
    </UserControl.Resources>

    <Grid VerticalAlignment="Stretch" ClipToBounds="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="12,0,0,12">
            <StackPanel Orientation="Horizontal">
                <Ellipse
                    Width="8"
                    Height="8"
                    Fill="{Binding DotColor, RelativeSource={RelativeSource AncestorType={x:Type local:ExpensesList}}}" />

                <TextBlock
                    Margin="12,0,0,0"
                    VerticalAlignment="Center"
                    FontFamily="{StaticResource Bold}"
                    Foreground="{StaticResource Brush.Text.Primary}"
                    Text="{Binding Title, RelativeSource={RelativeSource AncestorType={x:Type local:ExpensesList}}}" />
            </StackPanel>

            <TextBlock
                Margin="0,0,12,0"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                FontFamily="{StaticResource Light}"
                Foreground="{StaticResource Brush.Text.Secondary}"
                Text="{Binding SpentAmount, RelativeSource={RelativeSource AncestorType={x:Type local:ExpensesList}}, Converter={StaticResource NumberWithCommasConverter}, StringFormat={}${0}}" />
        </Grid>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Style="{StaticResource ExpenseListHeaderGridStyle}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Style="{StaticResource ExpenseListHeaderText}" Text="ITEM" />
                <TextBlock
                    Grid.Column="1"
                    HorizontalAlignment="Right"
                    Style="{StaticResource ExpenseListHeaderText}"
                    Text="AMOUNT" />
            </Grid>

            <Border
                Grid.Row="1"
                Height="1"
                Margin="12,0,12,12"
                Background="{StaticResource Brush.Text.Muted}"
                Opacity="0.25" />

            <Grid Grid.Row="2">
                <StackPanel
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Visibility="{Binding IsListEmpty, Converter={StaticResource BoolToVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type local:ExpensesList}}}">
                    <TextBlock
                        FontFamily="{StaticResource Bold}"
                        FontSize="14"
                        Foreground="{StaticResource Brush.Text.Muted}"
                        Text="No entry found" />
                </StackPanel>

                <customControls:FadingScrollViewer
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Style="{StaticResource ExpenseListFadingScrollViewerStyle}">
                    <ListView
                        ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType={x:Type local:ExpensesList}}}"
                        Style="{StaticResource ExpenseListViewStyle}" />
                </customControls:FadingScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</UserControl>