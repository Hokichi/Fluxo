<customControls:BasePopup
    x:Class="Fluxo.Views.Popups.QuickAddPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:components="clr-namespace:Fluxo.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:customControls="clr-namespace:Fluxo.Views.CustomControls"
    PopupTitle="Quick Add"
    mc:Ignorable="d">

    <customControls:BasePopup.Resources>
        <Style x:Key="QuickAddTileButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Width" Value="240" />
            <Setter Property="Height" Value="120" />
            <Setter Property="Padding" Value="18" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="TileBorder"
                            Padding="{TemplateBinding Padding}"
                            Background="{DynamicResource Brush.Background.Surface}"
                            BorderBrush="{DynamicResource Brush.Border.Subtle}"
                            BorderThickness="1"
                            CornerRadius="16"
                            SnapsToDevicePixels="True">
                            <ContentPresenter
                                x:Name="TileContent"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                RenderTransformOrigin="0.5,0.5">
                                <ContentPresenter.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1" />
                                </ContentPresenter.RenderTransform>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TileBorder" Property="Background" Value="{DynamicResource Brush.Background.Hover}" />
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation
                                                Storyboard.TargetName="TileContent"
                                                Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                To="1.02"
                                                Duration="0:0:0.14">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut" />
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation
                                                Storyboard.TargetName="TileContent"
                                                Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                To="1.02"
                                                Duration="0:0:0.14">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut" />
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation
                                                Storyboard.TargetName="TileContent"
                                                Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                To="1"
                                                Duration="0:0:0.14">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut" />
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation
                                                Storyboard.TargetName="TileContent"
                                                Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                To="1"
                                                Duration="0:0:0.14">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut" />
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </customControls:BasePopup.Resources>

    <Border Padding="20,6,20,20">
        <Grid Width="620">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                Margin="0,0,0,18"
                FontFamily="{DynamicResource Medium}"
                FontSize="12"
                Foreground="{DynamicResource Brush.Text.Secondary}"
                Text="Choose what you want to add."
                TextWrapping="Wrap" />

            <Grid Grid.Row="1" HorizontalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="16" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Button Grid.Row="0" Grid.Column="0" Click="OnNewSpendingSourceClick" Style="{StaticResource QuickAddTileButtonStyle}">
                    <StackPanel VerticalAlignment="Center">
                        <components:Icon Width="24" Height="24" HorizontalAlignment="Center" Path="{StaticResource QuickAddPopup.Bank}" Color="{DynamicResource Brush.Mint}" />
                        <TextBlock
                            Margin="0,12,0,0"
                            HorizontalAlignment="Center"
                            FontFamily="{DynamicResource Bold}"
                            FontSize="13"
                            Foreground="{DynamicResource Brush.Text.Primary}"
                            Text="New Spending Source"
                            TextAlignment="Center" />
                        <TextBlock
                            Margin="0,8,0,0"
                            HorizontalAlignment="Center"
                            FontFamily="{DynamicResource Medium}"
                            FontSize="11"
                            Foreground="{DynamicResource Brush.Text.Secondary}"
                            Text="Add a new account or wallet."
                            TextAlignment="Center"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </Button>

                <Button Grid.Row="0" Grid.Column="2" Click="OnNewTransactionClick" Style="{StaticResource QuickAddTileButtonStyle}">
                    <StackPanel VerticalAlignment="Center">
                        <components:Icon Width="24" Height="24" HorizontalAlignment="Center" Path="{StaticResource QuickAddPopup.InvoiceLight}" Color="{DynamicResource Brush.Mint}" />
                        <TextBlock
                            Margin="0,12,0,0"
                            HorizontalAlignment="Center"
                            FontFamily="{DynamicResource Bold}"
                            FontSize="13"
                            Foreground="{DynamicResource Brush.Text.Primary}"
                            Text="New Transaction"
                            TextAlignment="Center" />
                        <TextBlock
                            Margin="0,8,0,0"
                            HorizontalAlignment="Center"
                            FontFamily="{DynamicResource Medium}"
                            FontSize="11"
                            Foreground="{DynamicResource Brush.Text.Secondary}"
                            Text="Add income or expense quickly."
                            TextAlignment="Center"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </Button>

                <Button Grid.Row="2" Grid.Column="0" Click="OnNewSavingGoalClick" Style="{StaticResource QuickAddTileButtonStyle}">
                    <StackPanel VerticalAlignment="Center">
                        <components:Icon Width="24" Height="24" HorizontalAlignment="Center" Path="{StaticResource QuickAddPopup.BullseyeSolid}" Color="{DynamicResource Brush.Mint}" />
                        <TextBlock
                            Margin="0,12,0,0"
                            HorizontalAlignment="Center"
                            FontFamily="{DynamicResource Bold}"
                            FontSize="13"
                            Foreground="{DynamicResource Brush.Text.Primary}"
                            Text="New Saving Goals"
                            TextAlignment="Center" />
                        <TextBlock
                            Margin="0,8,0,0"
                            HorizontalAlignment="Center"
                            FontFamily="{DynamicResource Medium}"
                            FontSize="11"
                            Foreground="{DynamicResource Brush.Text.Secondary}"
                            Text="Set a target and track progress."
                            TextAlignment="Center"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </Button>

                <Button Grid.Row="2" Grid.Column="2" Click="OnNewFixedExpenseClick" Style="{StaticResource QuickAddTileButtonStyle}">
                    <StackPanel VerticalAlignment="Center">
                        <components:Icon Width="24" Height="24" HorizontalAlignment="Center" Path="{StaticResource QuickAddPopup.Calendar}" Color="{DynamicResource Brush.Mint}" />
                        <TextBlock
                            Margin="0,12,0,0"
                            HorizontalAlignment="Center"
                            FontFamily="{DynamicResource Bold}"
                            FontSize="13"
                            Foreground="{DynamicResource Brush.Text.Primary}"
                            Text="New Fixed Expenses"
                            TextAlignment="Center" />
                        <TextBlock
                            Margin="0,8,0,0"
                            HorizontalAlignment="Center"
                            FontFamily="{DynamicResource Medium}"
                            FontSize="11"
                            Foreground="{DynamicResource Brush.Text.Secondary}"
                            Text="Create recurring bills and dues."
                            TextAlignment="Center"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </Button>
            </Grid>
        </Grid>
    </Border>
</customControls:BasePopup>
