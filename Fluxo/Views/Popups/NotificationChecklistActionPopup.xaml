<customControls:BasePopup
    x:Class="Fluxo.Views.Popups.NotificationChecklistActionPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:customControls="clr-namespace:Fluxo.Views.CustomControls"
    Width="500"
    PopupTitle="Review Notifications">
    <customControls:BasePopup.Resources>
        <DataTemplate x:Key="NotificationChecklistItemTemplate">
            <CheckBox
                Margin="0,4,0,4"
                Content="{Binding Label}"
                IsChecked="{Binding IsSelected, Mode=TwoWay}" />
        </DataTemplate>
    </customControls:BasePopup.Resources>

    <Grid Margin="24,0,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            FontFamily="{StaticResource Bold}"
            FontSize="14"
            Foreground="{StaticResource Brush.Text.Primary}"
            Text="Select items to process" />

        <Border
            Grid.Row="2"
            Padding="12"
            Background="{StaticResource Brush.Background.Elevated}"
            BorderBrush="{StaticResource Brush.Border.Subtle}"
            BorderThickness="1"
            CornerRadius="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl
                    ItemTemplate="{StaticResource NotificationChecklistItemTemplate}"
                    ItemsSource="{Binding Items}" />
            </ScrollViewer>
        </Border>

        <Button
            Grid.Row="4"
            Height="36"
            Command="{Binding ProceedCommand}"
            Content="Proceed" />
    </Grid>
</customControls:BasePopup>
