<UserControl
    x:Class="Fluxo.Views.Shell.Main.Sections.FloatingNotificationList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:customControls="clr-namespace:Fluxo.Resources.CustomControls;assembly=Fluxo.Resources">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Fluxo.Resources;component/Resources/Styles/MainWindowStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <customControls:FadingScrollViewer
        x:Name="NotificationScrollViewer"
        Width="360"
        MaxHeight="243"
        HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Hidden">
        <ItemsControl ItemsSource="{Binding Items, Mode=OneWay}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border
                        Height="72"
                        Margin="8,4"
                        Padding="0"
                        Background="Transparent"
                        BorderBrush="{Binding Background, ElementName=AccentBackground}"
                        BorderThickness="2"
                        CornerRadius="12"
                        Cursor="Hand"
                        MouseLeftButtonUp="OnCardClick">
                        <Border.RenderTransform>
                            <TranslateTransform X="0" />
                        </Border.RenderTransform>
                        <Border.Effect>
                            <DropShadowEffect
                                BlurRadius="12"
                                Opacity="0.35"
                                ShadowDepth="2" />
                        </Border.Effect>
                        <Grid>
                            <Border
                                x:Name="AccentBackground"
                                CornerRadius="10"
                                Opacity="0.5"
                                Style="{StaticResource NotificationAccentStyle}" />
                            <StackPanel Margin="18,14">
                                <TextBlock
                                    FontFamily="{StaticResource Bold}"
                                    FontSize="15"
                                    Foreground="{StaticResource Brush.Text.Primary}"
                                    Text="{Binding Header, Mode=OneWay}" />
                                <TextBlock
                                    Margin="0,8,0,0"
                                    FontFamily="{StaticResource Regular}"
                                    FontSize="12"
                                    Foreground="{StaticResource Brush.Text.Secondary}"
                                    Text="{Binding Message, Mode=OneWay}"
                                    TextWrapping="Wrap" />
                                <ItemsControl Margin="0,6,0,0" ItemsSource="{Binding Details, Mode=OneWay}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock
                                                FontSize="11"
                                                Foreground="{StaticResource Brush.Text.Secondary}"
                                                Text="{Binding StringFormat=• {0}, Mode=OneWay}"
                                                TextWrapping="Wrap" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Grid>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Opacity" Value="1" />
                                <Style.Triggers>
                                    <EventTrigger RoutedEvent="Loaded">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation
                                                    Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                                    To="0"
                                                    Duration="0:0:0.25">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut" />
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                    <DataTrigger Binding="{Binding IsClosing, Mode=OneWay}" Value="True">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation
                                                        Storyboard.TargetProperty="Opacity"
                                                        To="0"
                                                        Duration="0:0:0.2" />
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </customControls:FadingScrollViewer>
</UserControl>
